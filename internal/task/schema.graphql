interface Node {
  id: ID!
}

type Task implements Node @key(fields: "id") {
  id: ID!
  title: String!
  user: User!
}

type Query {
  node(id: ID!): Node
  task(id: ID!): Task
  tasks: [Task!]!
}

type User implements Node {
  id: ID!
}
